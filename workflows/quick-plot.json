{
  "name": "quick-plot",
  "summary": "Generate a plot from a prompt and a target file",
  "description": "Single Claude call inside a Lambda. The processor reads the target file from EFS, asks the LLM Governor for a Python plotting script, runs it against the `quick-plot-stack` EFS layer, and attaches the resulting PNG as a viewer-asset on the target package. ~10s end-to-end.",
  "organizationId": "N:organization:050fae39-4412-43ef-a514-703ed8e299d5",
  "processors": [
    {
      "id": "target-file",
      "type": "data-source"
    },
    {
      "id": "quick-plot-processor",
      "type": "processor",
      "sourceUrl": "https://github.com/Pennsieve/pennsieve-quick-plot",
      "tag": "v0.1.0",
      "computeType": "lambda",
      "dependsOn": ["target-file"]
    },
    {
      "id": "figure-asset",
      "type": "data-target",
      "targetType": "Pennsieve Package Asset",
      "dependsOn": ["quick-plot-processor"],
      "defaultParams": {
        "ASSET_TYPE": "PNG",
        "ASSET_NAME": "quick-plot"
      }
    }
  ]
}
